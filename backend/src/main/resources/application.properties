# AWS Configuration
# Replace this with the name of your S3 bucket. You can find this value
# in the output of the 'terraform apply' command.
aws.s3.bucket.name=YOUR_S3_BUCKET_NAME_HERE

# The Spring Boot application will automatically use the default AWS credentials
# chain. For local development, ensure you have configured your credentials via
# the AWS CLI ('aws configure') or by setting the AWS_ACCESS_KEY_ID and
# AWS_SECRET_ACCESS_KEY environment variables.
